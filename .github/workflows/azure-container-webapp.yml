name: Azure Container Web App

# 已暂停自动 Azure 部署，避免影响股票量化扫描工作流。
# 如以后配置好 Azure，可在 Actions 中手动运行。
on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Show deployment placeholder
        run: echo "Azure deployment workflow is currently manual-only."
